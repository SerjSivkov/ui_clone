# TODO — UI Clone

План улучшений поверх текущего MVP: захват экрана (Android MediaProjection),
оверлей/уведомление «Стоп», vision-промпт или офлайн-шаблон.

Легенда: `[ ]` не начато · `[~]` в работе · `[x]` готово.

---

## Ближайшее (качество MVP)

### Захват и остановка

- [x] Умный захват: пропускать почти идентичные кадры (hash / diff %),
      чтобы не слать в AI дубликаты одного экрана.
- [x] Режим «по кнопке»: ручной снимок из оверлея (+ кадр) вместо только
      таймера.
- [ ] Режим видео: короткая запись экрана → ключевые кадры (MediaRecorder /
      extract frames), не только JPEG-таймер. *(отложено)*
- [x] Пауза / возобновление сбора из оверлея и уведомления.
- [x] Жёсткий лимит по времени сессии + предупреждение перед автостопом.
- [x] Не захватывать кадры, пока в foreground сам UI Clone (чёрный/свой UI).
- [ ] После нажатия стоп или окончания таймера перекидывать обратно в приложение UI Clone.
- [x] Accessibility / UsageStats: подсказка «сейчас открыто X», фильтр
      скриншотов только когда target package на переднем плане.

### Промпт и AI

- [ ] Редактор системного промпта в настройках (шаблоны: Flutter / React Native /
      Figma / общий UX).
- [ ] Структурированный ответ модели (JSON): палитра HEX, экраны, компоненты —
      плюс человекочитаемый markdown.
- [ ] Выбор провайдера: OpenAI · Anthropic · Gemini · свой OpenAI-compatible.
- [ ] Сжатие JPEG на клиенте перед upload (качество / max side в настройках).
- [ ] Прогресс анализа: сколько картинок ушло, ETA, отмена запроса.
- [ ] Повторный анализ той же сессии с другой моделью / промптом.
- [ ] Локальный vision (on-device / PocketForge) как опция без облака.

### Результат

- [ ] История сессий: список прошлых обзоров, скриншоты, промпт, дата.
- [ ] Экспорт: Markdown / JSON / ZIP (промпт + скриншоты).
- [ ] Превью скриншота на весь экран + выбор «включить / исключить из AI».
- [ ] Сравнение двух сессий (diff промптов / кадров).
- [ ] «Открыть в Cursor / скопировать для агента» — готовые пресеты текста.

---

## UX продукта

- [ ] Онбординг: 3 шага (разрешения → выбор app → стоп) при первом запуске.
- [ ] Проверка разрешений до старта: MediaProjection, уведомления, оверлей —
      чеклист со статусами.
- [ ] Тёмная тема + следование системной.
- [ ] Локализация: ru / en (сейчас тексты захардкожены на русском).
- [ ] Иконка приложения и splash (`flutter_launcher_icons`).
- [ ] Пустые и ошибочные состояния: нет приложений, отказ в записи экрана,
      пустой набор кадров — с понятным CTA.
- [ ] Haptic на стоп / готовность промпта.
- [ ] Фильтры в picker: только user apps / скрыть системные / недавние.

---

## Android (нативка)

- [ ] Стабильность MediaProjection на Android 14/15 (тип FGS, отзыв согласия).
- [ ] Кастомная иконка уведомления (монохром), не launcher mipmap.
- [ ] Оверлей: свернуть в точку / развернуть; не перекрывать важные зоны.
- [ ] Запрос `POST_NOTIFICATIONS` с объяснением до старта (не втихую).
- [ ] Политика `QUERY_ALL_PACKAGES`: fallback на launcher-intent без permission
      для Play; полный список — для sideload / debug.
- [ ] Unit/instrumentation-тесты для `ScreenCaptureService` (мок projection).

---

## iOS и другие платформы

- [ ] Честный iOS-flow: импорт скриншотов из Photos / Files → тот же AI-пайплайн
      (без захвата чужих app — ограничение ОС).
- [ ] ReplayKit только для своего процесса — задокументировать, не обещать
      клон чужого UI через screen record.
- [ ] Desktop (macOS): захват окна выбранного приложения (ScreenCaptureKit) —
      отдельный эпик.

---

## Данные и приватность

- [ ] Локальное хранение сессий (sqflite / drift), автоочистка кэша кадров.
- [ ] Опция «не отправлять скриншоты в облако» — только локальный шаблон /
      on-device.
- [ ] Redaction: размытие областей (статус-бар, аватар, чаты) перед upload.
- [ ] Экран политики: что снимается, куда уходит, как удалить данные.
- [ ] Шифрование API key уже есть (secure storage) — добавить «забыть ключ»
      и индикатор «ключ задан».

---

## Инфраструктура

- [ ] CI: `flutter analyze` + `flutter test` (GitHub Actions).
- [ ] Виджет-тесты: home, settings, result (мок platform channel).
- [ ] Fake `CapturePlatformService` для тестов без Android.
- [x] Скрипты релизной сборки APK/AAB (`scripts/flutter_build_release.sh`,
      `bin/ci/android_collect_artifacts.sh`, `docs/release-build.md`).
- [ ] Обновить `FEATURES.md` / `README.md` при закрытии крупных пунктов.
- [ ] Убрать/заменить pin-предупреждения AGP 9 / Built-in Kotlin у плагинов.

---

## Идеи на вырост

- [ ] Детекция экранов (кластеризация кадров) → «экран 1 / 2 / 3» в промпте.
- [ ] OCR текста на скриншотах (ML Kit) → подписи кнопок и заголовков в промпт.
- [ ] Генерация чернового Flutter-кода (скелет theme + 1–2 экрана) по промпту.
- [ ] Интеграция с Figma: экспорт описания / ссылка на frame.
- [ ] Шаринг сессии по ссылке (свой бэкенд) — опционально, не в MVP.
- [ ] Плагин Accessibility: дерево UI-нод целевого app (где ОС позволяет) для
      более точного layout, не только пиксели.
